# Handover — DTR-NQ-WP-20260721-02

## State

Implementation, full-data research, stress testing, independent review, and governance are complete.

Decision: `HOLD_FOR_FRESH_DATA`

Branch: `agent/nq-continuation-engine`

Draft PR: `#2 — Build independent NQ continuation engine`

## Delivered

- standalone continuation engine;
- one-bar and two-bar acceptance;
- immediate and first-pullback entry routes;
- pre-entry failure and expiry accounting;
- continuation-specific stop, TP1, runner, breakeven, failed-breakout, and event-end exits;
- gap-safe signal and trade handling;
- diagnostic fields for displacement, volume, extension, VWAP, ER, ADX, and timing;
- strict continuation manifest schema and checksum verification;
- manifest runner with deterministic CSV, Parquet, attribution, funnel, and JSON artifacts;
- structural and manifest fixtures;
- full baseline, timing, risk/exit, cost, session, bootstrap, and walk-forward evidence;
- independent review and final research report.

## Canonical commands

```bash
python scripts/run_continuation_manifest.py \
  configs/manifests/nq_continuation_structural_baseline.yaml
```

```bash
python scripts/run_continuation_manifest.py \
  configs/manifests/nq_continuation_late60_stress.yaml
```

Both canonical runs were repeated into clean directories and all generated artifacts were byte-identical.

## Research result

The four unfiltered structural variants are negative. The retained research lead is:

`CONT_A2_PULLBACK_LATE60`

Reference result:

- 147 trades;
- 0.108895R expectancy;
- 16.007565R net;
- PF 1.242960;
- 8.003633R maximum drawdown.

It remains held because its bootstrap intervals include zero, its aggregate result becomes negative at four-tick slippage, and no pristine fresh sample exists.

## Prohibited next actions

- Do not combine continuation with reversal.
- Do not optimize the late timing threshold further on the current dataset.
- Do not select a London-only or no-breakeven variant as a new candidate.
- Do not claim independent continuation profitability.

## Next roadmap item

Open a separate work package for IFVG entry-confirmation ablation. Test it independently against the frozen gap-safe reversal candidate and, only as a secondary diagnostic, against the held continuation lead. Preserve unfiltered opportunity coverage and removal attribution.
