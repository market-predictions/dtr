# Final Handover — DTR-NQ-WP-20260721-01

## Handover state

`COMPLETE`

Branch: `agent/nq-baseline-integrity-closure`

PR: `#1 — Close NQ baseline integrity gate`

Final promotion state: `PROMOTE_TO_CONTINUATION_RESEARCH`

## Executed dataset

- catalog ID: `nq_futures_1m_2022_2025`;
- SHA-256: `8d3f157a422636e5b8dda51cc3a3d9209c50cb53f9b279d3e14b627ce59370dc`;
- raw archive: intentionally excluded from Git.

## Locked baselines

### Frozen observe-only reference

- manifest: `configs/manifests/nq_candidate_0_1.yaml`;
- trades: `504`;
- net R: `84.16435914242919`;
- maximum drawdown: `14.107857513807524R`.

### Gap-safe baseline

- manifest: `configs/manifests/nq_candidate_0_1_gap_safe.yaml`;
- trades: `491`;
- net R: `88.49578342152539`;
- maximum drawdown: `14.107857513807524R`;
- tolerances: zero trades and `0.000001R` for net R and maximum drawdown.

## Difference attribution

- removed: `13` trades;
- added: `0` trades;
- contaminated session range: `9`;
- unsafe gap during open trade: `4`;
- setup-path-only removals: `0`;
- position-overlap consequences: `0`;
- unexplained: `0`.

Detailed trade rows:

`results/2026-07-21/nq_candidate_0_1_gap_safe_removed_trades.csv`

## Determinism

Both manifests were run twice into clean directories. Every required artifact was byte-identical:

- `trades.csv`;
- `trades.parquet`;
- `funnel.csv`;
- `halfyear.csv`;
- `by_session.csv`;
- `by_weekday.csv`;
- `by_direction.csv`;
- `summary.json`.

Artifact hashes:

`results/2026-07-21/nq_candidate_0_1_gap_safe_summary.json`

## Interpretation

The sanitized result is descriptively stronger overall, but this is not an optimization gain. Missing-data-contaminated observations were removed. New York performance declines after sanitization, and the later-research improvement is small. The correct conclusion is that the baseline is now cleaner and reproducible—not that the edge has strengthened.

## Open risks retained

- timestamp semantics;
- daylight-saving and session-boundary verification;
- continuous-contract rollover and adjustment methodology;
- exact RTH/ETH VWAP reset semantics;
- absence of a post-December-2025 paper-forward sample.

## Next handover target

`DTR-NQ-WP-20260721-02 — Independent continuation engine`

Required constraints:

1. use `reject_unsafe` by default;
2. keep continuation separate from reversal;
3. define accepted breakout and failed-breakout invalidation from first principles;
4. report the complete setup funnel;
5. test immediate and first-pullback entries separately;
6. require chronological walk-forward evidence before combination;
7. do not retune the frozen reversal candidate inside the continuation work package.
