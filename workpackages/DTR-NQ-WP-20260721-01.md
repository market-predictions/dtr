# DTR-NQ-WP-20260721-01 — Baseline integrity closure

## Status

`IMPLEMENTED_PENDING_FULL_DATASET_RERUN`

## Owner

ChatGPT autonomous development agent

## Goal

Close the NQ data-integrity and reproducibility gate before any further parameter optimization. The frozen reversal candidate must be protected from setup formation or trade simulation across unsafe market-data gaps.

## Scope completed

1. Preserved the existing pre-safety reference baseline and configuration.
2. Annotated derived five-minute data with deterministic reset and unsafe-gap epochs.
3. Rejected contaminated session ranges and truncated post-range signal paths at reset boundaries.
4. Rejected open trades that overlap unsafe missing-data intervals from primary gap-safe results.
5. Added explicit funnel counters for data-integrity observations and rejections.
6. Added unit and regression tests for intra-bucket gaps, range gaps, signal-path gaps, trade overlaps, clean-data behavior, and manifest policy separation.
7. Ran pinned Ruff plus pytest on Python 3.11 and 3.12.
8. Performed an independent adversarial review.
9. Updated changelog, roadmap, README, status, claim, review, and handover artifacts.

## Non-goals respected

- No parameter retuning.
- No continuation-engine implementation.
- No Dukascopy or additional-feed implementation.
- No production-performance claim.
- No replacement of the historical 504-trade reference result.

## Acceptance status

- [x] Existing clean-data behavior remains deterministic in unit and regression tests.
- [x] A session range intersecting an unsafe gap cannot produce a gap-safe signal.
- [x] A sweep/reclaim/BOS/acceptance path cannot continue across a reset boundary.
- [x] An open trade intersecting an unsafe gap is excluded and counted explicitly.
- [x] Scheduled maintenance and standard weekend closures remain classified separately from unsafe gaps.
- [x] Tests cover gaps within a five-minute bucket, between buckets, and gaps that resume after the affected window ends.
- [x] CI passes on Python 3.11 and 3.12.
- [x] Independent review records the promotion decision and unresolved limitations.
- [ ] The checksum-matched full NQ archive reproduces the 504-trade observe-only reference.
- [ ] The gap-safe full-dataset artifacts and changed-trade attribution are generated and locked.

## Review findings incorporated

1. The historical candidate and sanitized candidate require separate explicit manifests. The reference uses `observe_only`; the sanitized run uses `reject_unsafe` with identical strategy parameters.
2. Safety must evaluate complete missing-data intervals, not only the timestamp at which data resumes.
3. Ordinary optimization entry points must route through the gap-safe runner, while the captured legacy runner remains available only for reproducibility of the frozen reference.
4. Ruff is pinned and separated from the Python test matrix; narrowly scoped legacy lint debt is recorded rather than hidden globally.

## Current gate

Implementation is complete and CI is green. Promotion is limited to:

`PROMOTE_TO_FULL_DATASET_RERUN`

The work package is not yet promoted to continuation research or candidate performance approval.

## Remaining execution dependency

The raw archive is intentionally excluded from Git. The next operator must place the checksum-matched file at:

`data/raw/NQ_Futures_-_1min_Bar_2022_2025.zip`

and execute both versioned manifests described in the handover.
