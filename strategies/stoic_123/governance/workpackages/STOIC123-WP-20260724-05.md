# Work Package — USA500 RTH Long Cross-Asset Replication

Work package: `STOIC123-WP-20260724-05`
Date opened: 2026-07-24
Branch: `agent/stoic-usa500-rth-long-replication`
Status: `SOURCE_QUALIFICATION_PENDING`

## Strategic question

How do the already frozen NQ RTH-long settings perform when transferred unchanged to Dukascopy USA500, an S&P 500 bid-CFD proxy?

## Scientific boundary

This is a cross-asset replication, not a new optimization programme. The complete entry, management, execution, cost and session definitions from `STOIC123-WP-20260724-04` remain unchanged. Only the source identity and ES-equivalent research economics change.

The result must remain labelled `ES_PROXY` / Dukascopy USA500 and cannot be represented as CME ES futures evidence.

## Frozen session contract

- Source timestamps: UTC.
- Session timezone: `America/New_York` with IANA daylight-saving rules.
- RTH entry window: `09:30:00` inclusive through `16:00:00` exclusive.
- The filter applies only to entry signals.
- Stops, opposite-direction technical exits and maximum-hold exits remain full-session.
- No RTH subwindow or intraday-hour search is permitted.

## Frozen candidate family

### Primary

`RTH_LONG_EMA_BREAK`

- no-map configuration;
- long entries only;
- unchanged Step-1 EMA-break definition;
- next-open execution;
- both-direction management retained.

### Secondary

`RTH_LONG_FULL_123`

- unchanged full Step 1, retest, locked base and Step 3;
- long entries only;
- both-direction management retained.

### Diagnostic

`RTH_LONG_EMA_BREAK_RETEST`

The retest arm is diagnostic only.

## Source qualification

Before performance execution, qualify Dukascopy `USA500IDXUSD` BID M1 data for:

- fresh history: 2012–2013;
- untouched holdout: 2014.

For each partition publish deterministic compressed-file SHA-256, row count, timestamp bounds, zero-volume removal, duplicate count, OHLC integrity and explicit proxy classification.

## ES-equivalent research economics

- execution tick: 0.25 points;
- point value: USD 50 per point;
- commission: USD 2.25 per side;
- baseline slippage: one tick per side;
- stress slippage: two ticks per side.

These are research-normalization assumptions, not a claim about Dukascopy CFD contract economics or CME fills.

## Validation

Run the exact same scenario family as the NQ-proxy RTH study:

- full-session full sequence;
- overnight-entry full sequence;
- RTH full sequence;
- RTH EMA break;
- RTH EMA break plus retest;
- two-tick stress;
- one-minute and five-minute delays;
- annual, exposure, exit and date-block attribution;
- fifty deterministic RTH matched-time controls for primary and secondary candidates;
- independent ledger and gate reconstruction.

## Decision boundary

This package may classify the USA500 transfer as positive, uncertain or rejected. It cannot authorize Pine, sizing, alerts, paper deployment, live trading or an ES-futures claim.

The full sequence must add at least `0.05R` expectancy or 20% return-to-drawdown over EMA-break-only on fresh history and must not underperform on the 2014 holdout.

## Restrictions

- No change to the phase-one configuration.
- No session, weekday, volatility, EMA-length, base, retest, stop, target, delay, timeframe, exit or matching-rule search.
- No pooling with NQ or USATECH returns.
- No use of DTR or Asian Sweep results to alter this Stoic replication.
- No raw market-data publication.
