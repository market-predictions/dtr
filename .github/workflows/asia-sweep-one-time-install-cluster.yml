name: One-time Install Cluster Challenger

on:
  push:
    branches:
      - agent/asia-sweep-pdh-pdl-cluster-challenger
    paths:
      - .github/workflows/asia-sweep-one-time-install-cluster.yml

permissions:
  contents: write

jobs:
  install-and-remove:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: agent/asia-sweep-pdh-pdl-cluster-challenger
          show-progress: false

      - name: Reconstruct and verify package
        shell: bash
        run: |
          set -euo pipefail
          cat .cluster_payload/part_* > /tmp/cluster_install.b64
          base64 -d /tmp/cluster_install.b64 > /tmp/cluster_install.tar.gz
          echo "d727602b1ede6997d9267f664b9712e94e6138c5d41c863bd71fd8ad5fb26d0c  /tmp/cluster_install.tar.gz" | sha256sum -c -
          tar -xzf /tmp/cluster_install.tar.gz -C .
          rm -rf .cluster_payload
          rm .github/workflows/asia-sweep-one-time-install-cluster.yml

      - name: Validate installed package
        run: |
          python -m py_compile \
            src/dtr_lab/strategies/asia_sweep/cluster_challenger.py \
            scripts/run_asia_sweep_cluster_challenger.py \
            scripts/aggregate_asia_sweep_cluster_challenger.py \
            strategies/asia_sweep/tests/test_cluster_challenger.py
          PYTHONPATH=src pytest -c strategies/asia_sweep/pytest.ini \
            strategies/asia_sweep/tests/test_cluster_challenger.py

      - name: Commit installation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Add frozen PDH PDL cluster challenger"
          git push origin HEAD:agent/asia-sweep-pdh-pdl-cluster-challenger
