name: Asian Sweep Staged Exit Discovery

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  actions: read

concurrency:
  group: asia-sweep-staged-exit-discovery-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  QUALIFIED_SOURCE_RUN_ID: "30111481052"
  DEVELOPMENT_ANATOMY_RUN_ID: "30174245825"
  EXECUTION_DISCOVERY_RUN_ID: "30192167763"

jobs:
  focused_tests:
    name: Validate frozen staged-exit contract
    if: >-
      github.event_name == 'workflow_dispatch' ||
      github.head_ref == 'agent/asia-sweep-staged-exit-research'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          show-progress: false
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install development dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
      - name: Run staged-exit falsification tests
        run: pytest strategies/asia_sweep/tests/test_staged_exit_strategy.py
      - name: Lint staged-exit implementation
        run: |
          ruff check \
            src/dtr_lab/strategies/asia_sweep/staged_exit_simulator.py \
            src/dtr_lab/strategies/asia_sweep/staged_exit_metrics.py \
            scripts/run_asia_sweep_staged_exit_pair.py \
            scripts/aggregate_asia_sweep_staged_exit_discovery.py \
            strategies/asia_sweep/tests/test_staged_exit_strategy.py

  pair_staged_exit:
    name: Build staged-exit discovery ${{ matrix.symbol }}
    needs: focused_tests
    runs-on: ubuntu-latest
    timeout-minutes: 120
    strategy:
      fail-fast: false
      max-parallel: 2
      matrix:
        symbol: [EURUSD, GBPUSD]
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          show-progress: false
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e .
      - name: Restore qualified FX source cache
        id: source-cache
        uses: actions/cache/restore@v4
        with:
          path: ${{ runner.temp }}/source/${{ matrix.symbol }}
          key: stacey-burke-fx-v1-${{ matrix.symbol }}-2015-2026-07-24
      - name: Download qualified source on cache miss
        if: steps.source-cache.outputs.cache-hit != 'true'
        uses: actions/download-artifact@v4
        with:
          name: stacey-burke-fx-source-${{ matrix.symbol }}
          path: ${{ runner.temp }}/source/${{ matrix.symbol }}
          repository: market-predictions/dtr
          run-id: ${{ env.QUALIFIED_SOURCE_RUN_ID }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
      - name: Download immutable development event ledger
        uses: actions/download-artifact@v4
        with:
          name: asia-sweep-fingerprint-development-${{ matrix.symbol }}
          path: ${{ runner.temp }}/events/${{ matrix.symbol }}
          repository: market-predictions/dtr
          run-id: ${{ env.DEVELOPMENT_ANATOMY_RUN_ID }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
      - name: Download frozen market-entry execution ledger
        uses: actions/download-artifact@v4
        with:
          name: asia-sweep-execution-discovery-${{ matrix.symbol }}
          path: ${{ runner.temp }}/execution/${{ matrix.symbol }}
          repository: market-predictions/dtr
          run-id: ${{ env.EXECUTION_DISCOVERY_RUN_ID }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
      - name: Enforce staged-exit discovery inputs
        shell: bash
        run: |
          set -euo pipefail
          symbol="${{ matrix.symbol }}"
          lower="${symbol,,}"
          for year in 2015 2016 2017 2018 2019; do
            test -s "${RUNNER_TEMP}/source/${symbol}/${lower}_m1_bid_${year}.csv.gz"
            test -s "${RUNNER_TEMP}/source/${symbol}/${lower}_m1_ask_${year}.csv.gz"
          done
          test -s "${RUNNER_TEMP}/events/${symbol}/fingerprint_events.csv"
          test -s "${RUNNER_TEMP}/execution/${symbol}/execution_orders.csv"
      - name: Simulate frozen staged-exit family
        run: |
          mkdir -p "${RUNNER_TEMP}/staged/${{ matrix.symbol }}"
          python scripts/run_asia_sweep_staged_exit_pair.py \
            --data "${RUNNER_TEMP}/source/${{ matrix.symbol }}" \
            --execution-orders "${RUNNER_TEMP}/execution/${{ matrix.symbol }}/execution_orders.csv" \
            --events "${RUNNER_TEMP}/events/${{ matrix.symbol }}/fingerprint_events.csv" \
            --symbol "${{ matrix.symbol }}" \
            --start-year 2015 \
            --end-year 2019 \
            --out "${RUNNER_TEMP}/staged/${{ matrix.symbol }}"
      - name: Upload pair staged-exit evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: asia-sweep-staged-exit-discovery-${{ matrix.symbol }}
          path: ${{ runner.temp }}/staged/${{ matrix.symbol }}
          if-no-files-found: warn
          retention-days: 90

  discovery_gate:
    name: Enforce staged-exit discovery gate
    needs: pair_staged_exit
    if: always() && needs.pair_staged_exit.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          show-progress: false
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e .
      - name: Download pair staged-exit evidence
        uses: actions/download-artifact@v4
        with:
          pattern: asia-sweep-staged-exit-discovery-*
          path: ${{ runner.temp }}/staged-inputs
          merge-multiple: false
      - name: Aggregate frozen staged-exit discovery gate
        run: |
          python scripts/aggregate_asia_sweep_staged_exit_discovery.py \
            --inputs "${RUNNER_TEMP}/staged-inputs" \
            --out "${RUNNER_TEMP}/staged-decision"
      - name: Upload complete staged-exit decision
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: asia-sweep-staged-exit-discovery-decision
          path: ${{ runner.temp }}/staged-decision
          if-no-files-found: warn
          retention-days: 90
      - name: Enforce frozen staged-exit scientific decision
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          decision="${RUNNER_TEMP}/staged-decision/staged_exit_discovery_decision.json"
          test -s "${decision}"
          python - "${decision}" <<'PY'
          import json
          import sys
          from pathlib import Path

          payload = json.loads(Path(sys.argv[1]).read_text(encoding="utf-8"))
          print(json.dumps(payload, indent=2, sort_keys=True))
          expected = "PASS_STAGED_EXIT_DISCOVERY_AUTHORIZE_2020_2021_VALIDATION"
          raise SystemExit(0 if payload.get("decision") == expected else 1)
          PY
