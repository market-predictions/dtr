name: Cancel Stale Quarters Run

on:
  push:
    branches:
      - agent/quarters-theory-crosspair
    paths:
      - .github/workflows/cancel-stale-quarters.yml

permissions:
  actions: write
  contents: read

jobs:
  cancel:
    runs-on: ubuntu-latest
    steps:
      - name: Cancel obsolete high-concurrency run
        uses: actions/github-script@v7
        with:
          script: |
            try {
              await github.rest.actions.cancelWorkflowRun({
                owner: context.repo.owner,
                repo: context.repo.repo,
                run_id: 31018008730,
              });
              core.info('Cancellation requested for stale run 31018008730.');
            } catch (error) {
              if (error.status === 409) {
                core.info('Stale run is already completed or cancelling.');
              } else {
                throw error;
              }
            }
